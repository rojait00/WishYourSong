@page "/"
@using WishYourSong.Components

<PageTitle>Search Song</PageTitle>

<SearchResults @bind-value=Term></SearchResults>

@if (string.IsNullOrWhiteSpace(Term))
{
    <Songlist></Songlist>
}


@code
{
    string Term { get; set; } = "";
}
